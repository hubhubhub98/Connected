<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.changsdev.whoaressuproject">
    <!-- 앱에서 전화 연결을 위한 사용 허가 -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ssu"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ssu"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".PlaceIndicateMarkerActivity"></activity>
        <activity android:name=".model.loading_activity"></activity>
        <activity android:name=".activity_chatroom"></activity>
        <activity android:name=".PlaceViewActivity"></activity>
        <activity android:name=".ManagerRegisterActivity"></activity>
        <activity android:name=".PlaceMapViewActivity" />
        <activity android:name=".PlaceListActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDtyUxExkybeuzsJrzFRT6rsVDqFsPt7wk" />

        <activity android:name=".PlaceWriteActivity" />
        <activity android:name=".GoogleMapTestActivity" />
        <activity android:name=".PwResetActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan"></activity>

    </application>

</manifest>